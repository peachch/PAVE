# Every model is reached through one OpenAI-compatible client, so no
# provider-specific SDK is needed. For a local model, point OPENAI_BASE_URL at
# the server's OpenAI-compatible endpoint (e.g. http://localhost:11434/v1).
openai>=1.0
pandas>=1.3      # only for counter.py --dump-csv
tqdm
